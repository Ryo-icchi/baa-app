# baa-app

- 1歳児向け「いないいないばあ」PWA。バニラ JS・ビルドなし・GitHub Pages 配信（`deploy.sh` で公開）
- 変更後は `node test-e2e.mjs` を実行し、ブラウザでも実操作して確認する
- 音・マナーモード等 iPhone 実機依存の機能は「動作OK」と断定せず「実機確認待ち」と報告する（過去に早合点で翌日破綻）
- 子供向け UI: 黒は使わない・状態はテキストラベルより視覚表現で示す
- 公開 = main への push で GitHub Pages に反映（`deploy.sh` は初期セットアップ兼用）。反映後は本番 URL を開いて確認
